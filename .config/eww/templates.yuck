(defwidget info-box [?classname]
    (box 
        :class "widget ${classname}"
        :hexpand false
        :vexpand false
        :width 400
        :height 140
        ;:orientation {ori == 'h' ? 'h' : 'v'}
        (children)
    )
)

